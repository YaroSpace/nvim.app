CREATE TABLE github (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  stargazers INTEGER NOT NULL DEFAULT 0
);

--;;

ALTER TABLE plugins
ADD COLUMN github_id INTEGER REFERENCES github(id);
